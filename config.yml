title: Bootstrap
description: Utilizando bootstrap
template:
  repo: bootstrap-template
  name: bootstrap
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
